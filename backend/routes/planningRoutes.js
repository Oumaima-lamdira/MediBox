const express = require('express'); const router = express.Router(); const Planning = require('../models/Planning'); router.get('/', async (req, res) => { try { const { id_client } = req.query; if (!id_client) { return res.status(400).json({ message: 'L ID du client est requis' }); } const plannings = await Planning.getAll(id_client); res.json(plannings); } catch (error) { res.status(500).json({ message: 'Erreur serveur', error: error.message }); } }); module.exports = router;
