const express = require('express'); const router = express.Router(); const Medicament = require('../models/Medicament'); router.get('/', async (req, res) => { try { const { id_client } = req.query; if (!id_client) { return res.status(400).json({ message: 'L ID du client est requis' }); } const medicaments = await Medicament.getAllWithStockStatus(id_client); res.json(medicaments); } catch (error) { res.status(500).json({ message: 'Erreur serveur', error: error.message }); } }); module.exports = router;
